// Empty stub for node-only optional dependencies of sleap-io.js (e.g. skia-canvas).
// These are reached only by Node/Electron code paths, which never execute in the
// browser SPA. Aliasing them here keeps them out of the client bundle.
export default {};
